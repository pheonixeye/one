import 'package:flutter/material.dart';

class NotFoundPatientsPortalPage extends StatelessWidget {
  const NotFoundPatientsPortalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('not_found_patient_information'),
      ),
    );
  }
}
