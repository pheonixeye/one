import 'package:flutter/material.dart';

class BookVisitDialog extends StatelessWidget {
  const BookVisitDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
